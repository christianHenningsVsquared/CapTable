{
  "name": "captable",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VC cap-table tool — MVP (Stream A: ingestion + DB)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.97.1",
    "better-sqlite3": "^12.10.0",
    "decimal.js": "^10.6.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
